# backend/docker-compose.yml
version: '3.8'

services:
  mongodb:
    image: mongo:6
    container_name: unishare-mongo
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - ./data:/data/db
    environment:
      MONGO_INITDB_DATABASE: unishare
